name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Use specific, supported minor versions. Avoid typos like '3.1' (which is NOT '3.11').
        python-version: [3.12, 3.11]
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Check Python version (debug)
        shell: bash
        run: |
          echo "Python setup:"
          python --version || true
          which python || true

      - name: Install dependencies (if any)
        shell: bash
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            echo "No requirements.txt found — skipping dependency installation"
          fi

      - name: Optional lint (flake8)
        shell: bash
        run: |
          if command -v flake8 >/dev/null 2>&1; then
            echo "Running flake8..."
            flake8 .
          else
            echo "flake8 not installed — skipping lint"
          fi

      - name: Run session scripts
        shell: bash
        run: |
          echo "Running session1.py"
          python session1.py
          echo "Running session2.py"
          python session2.py
